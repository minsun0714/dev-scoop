spring:
  datasource:
    url: jdbc:mysql://localhost:3306/devscoop?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: minsun
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: false
